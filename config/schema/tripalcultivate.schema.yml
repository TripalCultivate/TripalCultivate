# Tripal Cultivate Configuration/Settings Schema.

tripalcultivate.settings:
  type: config_object
  mapping:
    tripalcultivate:
      type: mapping
      label: 'Tripal Cultivate Configuration'
      mapping:
        directory:
          type: mapping
          label: 'Directory structure'
          mapping:
            template_file:
            type: path
            label: 'Directory: data collection template file'